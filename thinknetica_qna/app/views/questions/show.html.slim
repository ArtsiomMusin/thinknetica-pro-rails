h2= @question.title
p= text_area_tag @question.title, @question.body, rows: 10, cols: 25

h3 Answers
- @question.answers.each do |answer|
  p= text_area_tag 'answer', answer.body, rows: 5, cols: 10
- if @question.answers.count == 0
  p No answers yet

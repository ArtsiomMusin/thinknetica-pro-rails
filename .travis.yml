language: ruby
rvm:
  - 2.2.2
before_install:  cd thinknetica_qna

addons:
  postgresql: "9.4"

script:
  - export RAILS_ENV=test
  - bundle exec rake db:create db:schema:load db:test:prepare
  - bundle exec rspec --color --require spec_helper
